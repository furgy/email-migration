[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "email-migration"
version = "0.1.0"
description = "Email migration using imapsync"
requires-python = ">=3.8"
